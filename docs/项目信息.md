# 绳网论坛 项目信息

## 项目概述

绳网论坛（InterKnot Forum）是一个复刻《绝区零》游戏绳网风格的论坛系统，采用前后端分离架构，可部署到 Netlify 平台。

## 基本信息

| 项目 | 说明 |
|------|------|
| 项目名称 | 绳网论坛（InterKnot Forum） |
| 版本 | 0.2.0 |
| 仓库地址 | https://github.com/raven-null/inter-knot |
| 线上地址 | https://interknot.netlify.app |
| 技术栈 | Nuxt 4 + Vue 3 + Netlify Functions + Netlify Blobs |
| 开发语言 | TypeScript |
| 包管理器 | npm |

---

## 目录结构总览

```
inter-knot/
├── app/                          # Nuxt 前端源码
├── netlify/                      # Netlify 后端
│   └── functions/                # Serverless 函数
├── zzzui/                        # 自研 UI 组件库
├── public/                       # 静态资源
├── docs/                         # 项目文档
├── .nuxt/                        # Nuxt 自动生成（构建产物）
├── .output/                      # 构建输出目录
├── node_modules/                 # 依赖包
├── netlify.toml                  # Netlify 部署配置
├── nuxt.config.ts                # Nuxt 配置文件
├── package.json                  # 项目依赖配置
├── tsconfig.json                 # TypeScript 配置
├── vitest.config.ts              # 测试配置
├── README.md                     # 项目说明
└── SECURITY.md                   # 安全说明
```

---

## 各文件夹详细说明

### `app/` - Nuxt 前端源码

前端应用的主目录，Nuxt 4 的 `srcDir` 配置指向此目录。

```
app/
├── app.vue                       # 根组件（全局布局、背景跑马灯）
├── error.vue                     # 错误页面
├── router.options.ts             # 路由配置
├── spa-loading-template.html     # SPA 加载模板
│
├── pages/                        # 页面路由（基于文件的路由系统）
│   ├── index.vue                 # 首页（信息流、搜索、版块筛选）
│   ├── create.vue                # 发帖页（Markdown 编辑器、图片上传）
│   ├── knock.vue                 # 敲敲入口（触发弹窗）
│   ├── level.vue                 # 等级体系页
│   ├── account.vue               # 账号中心（密钥、绑定、黑名单）
│   ├── [...slug].vue             # 404 兜底页面
│   ├── post/
│   │   └── [id].vue              # 帖子详情页（正文、评论、互动）
│   ├── profile/
│   │   └── [id].vue              # 个人主页（名片、统计、帖子、通知）
│   └── admin/                    # 后台管理页面
│       ├── index.vue             # 数据概览（统计卡片、趋势图）
│       ├── users.vue             # 用户管理（搜索、添加、禁用、删除）
│       ├── posts.vue             # 帖子管理（置顶、下架、删除）
│       ├── comments.vue          # 评论管理（搜索、删除）
│       ├── reports.vue           # 举报管理（处理举报）
│       ├── categories.vue        # 版块管理（新增、编辑、删除）
│       ├── forum.vue             # 导航与功能开关、公告、表情包管理
│       └── settings.vue          # 站点设置（名称、公告、注册开关）
│
├── components/                   # Vue 组件（45+）
│   ├── # 核心业务组件
│   ├── KnockKnockModal.vue       # 敲敲弹窗主组件（私聊/群聊/AI，4000+ 行）
│   ├── PostOverlay.vue           # 帖子弹窗（首页点击打开，3000+ 行）
│   ├── DmMessageItem.vue         # 私聊消息气泡（文本/图片/视频）
│   ├── DmComposer.vue            # 私聊输入框（工具栏、表情、图片）
│   ├── CommentItem.vue           # 评论组件（楼中楼）
│   ├── CommentBody.vue           # 评论正文渲染
│   ├── PostCard.vue              # 帖子卡片（信息流展示）
│   ├── UserHoverCard.vue         # 用户悬浮名片
│   │
│   ├── # 提及与表情
│   ├── MentionPicker.vue         # @提及候选浮层
│   ├── MentionChip.vue           # @提及芯片（评论中显示）
│   ├── MentionHighlightOverlay.vue # @提及高亮叠加层
│   ├── EmotePicker.vue           # 表情选择面板（桌面端）
│   ├── MobileEmotePicker.vue     # 表情选择面板（移动端）
│   ├── EmoteImage.vue            # 表情图片组件
│   │
│   ├── # 媒体相关
│   ├── BilibiliPlayer.vue        # B站视频播放器
│   ├── BilibiliVideoDialog.vue   # B站视频添加对话框
│   ├── NsfwImage.vue             # NSFW 敏感内容图片组件
│   ├── PostImagePickerModal.vue  # 图片选择弹窗（图库+上传）
│   ├── CoverImageAddButton.vue   # 添加封面图片按钮
│   ├── CoverVideoAddButton.vue   # 添加封面视频按钮
│   │
│   ├── # 用户相关
│   ├── LoginDialog.vue           # 登录对话框
│   ├── ProfileSettingsModal.vue  # 个人设置弹窗（用户名、签名、头像、名片）
│   ├── AvatarModal.vue           # 头像选择弹窗
│   ├── BusinessCardModal.vue     # 名片选择弹窗
│   ├── PinnedArticlesModal.vue   # 置顶帖子选择弹窗
│   ├── ReportModal.vue           # 举报弹窗
│   │
│   ├── # 布局与导航
│   ├── AppHeader.vue             # 顶部导航栏
│   ├── AppFooter.vue             # 页脚
│   ├── MobileBottomNav.vue       # 移动端底部导航
│   ├── HomeSearchBar.vue         # 首页搜索框
│   │
│   ├── # 后台管理组件
│   ├── AdminBackButton.vue       # 后台返回按钮
│   ├── AdminBadge.vue            # 后台徽章组件
│   ├── AdminCard.vue             # 后台卡片组件
│   ├── AdminDrawer.vue           # 后台抽屉组件
│   ├── AdminEmpty.vue            # 后台空状态组件
│   │
│   ├── # 通用组件
│   ├── ConfirmDialog.vue         # 确认对话框
│   ├── RelativeTime.vue          # 相对时间显示
│   ├── IkRollingDigit.vue        # 滚动数字组件
│   ├── IkZzzMarquee.vue          # 跑马灯组件
│   ├── KnockDragBadge.vue        # 拖拽徽章
│   ├── VirtualMasonry.vue        # 虚拟瀑布流
│   ├── PostCardSkeleton.vue      # 帖子卡片骨架屏
│   │
│   └── ai/                       # AI 相关组件
│       ├── AiMessageBody.vue     # AI 消息正文
│       ├── AiReasoningBlock.vue  # AI 推理过程展示
│       ├── AiCitationList.vue    # AI 引用列表
│       └── AiRelatedPosts.vue    # AI 推荐帖子
│
├── composables/                  # 组合式函数（33+）
│   ├── # API 层
│   ├── useApi.ts                 # 前端 API 层（所有 HTTP 请求）
│   ├── useAdminApi.ts            # 后台管理 API
│   │
│   ├── # 认证与用户
│   ├── useAccountData.ts         # 账号数据
│   ├── useLoginDialog.ts         # 登录对话框状态
│   ├── useMihoyoQr.ts            # 米游社扫码登录
│   │
│   ├── # 帖子与评论
│   ├── usePostModal.ts           # 帖子弹窗状态
│   ├── usePostPrefetch.ts        # 帖子预加载
│   ├── useCommentImages.ts       # 评论图片上传
│   ├── useCommentSeek.ts         # 评论定位
│   ├── usePendingPost.ts         # 待发布帖子
│   │
│   ├── # 私信系统
│   ├── useDmConversations.ts     # 私聊会话管理（CRUD、消息收发）
│   ├── useDmStream.ts            # 私聊消息流（轮询）
│   ├── useKnockKnockConversations.ts # 敲敲会话列表
│   ├── useKnockKnockModal.ts     # 敲敲弹窗状态
│   │
│   ├── # 输入增强
│   ├── useMentionInput.ts        # @提及输入逻辑
│   ├── useEmoteInsert.ts         # 表情插入逻辑
│   ├── useEmotes.ts              # 表情包数据
│   │
│   ├── # 媒体与展示
│   ├── useLightGallery.ts        # 灯箱 composable
│   ├── useVideoMuted.ts          # 视频静音状态
│   ├── useRenderedBody.ts        # 渲染正文
│   ├── useGpuAccelerated.ts      # GPU 加速
│   ├── useHoverCapable.ts        # 悬停能力检测
│   │
│   ├── # 社交功能
│   ├── usePresence.ts            # 在线状态
│   ├── useReportDialog.ts        # 举报对话框
│   │
│   ├── # AI 功能
│   ├── useAiCharacters.ts        # AI 角色列表
│   ├── useAiDmTypewriter.ts      # AI 打字机效果
│   │
│   ├── # 通知
│   ├── useConfirmDialog.ts       # 确认对话框
│   │
│   ├── # 缓存与状态
│   ├── useHomeStateCache.ts      # 首页状态缓存
│   ├── usePageDataLoading.ts     # 页面数据加载
│   ├── useSearchHistory.ts       # 搜索历史
│   ├── useSiteSettings.ts        # 站点设置
│   ├── useBodyScrollLock.ts      # 滚动锁定
│   └── useVisualViewport.ts      # 可视视口
│
├── stores/                       # Pinia 状态管理
│   └── auth.ts                   # 认证状态（用户信息、登录态）
│
├── types/                        # TypeScript 类型定义
│   └── entities.ts               # 实体类型（Post、Comment、User、DmMessage 等）
│
├── utils/                        # 工具函数
│   ├── api-error.ts              # API 错误处理
│   ├── cover.ts                  # 封面比例计算
│   ├── dm-view.ts                # DM 消息视图工具
│   ├── emote.ts                  # 表情 token 解析
│   ├── image.ts                  # 图片 URL 处理（缩略图、CDN）
│   ├── level.ts                  # 等级计算
│   ├── mention.ts                # Mention token 解析/构建
│   ├── pagination.ts             # 分页工具
│   ├── request-auth.ts           # 请求认证
│   ├── time.ts                   # 时间格式化
│   ├── upload.ts                 # 上传常量与客户端压缩
│   └── workflow.ts               # AI 工作流步骤解析
│
├── layouts/                      # 布局组件
│   └── admin.vue                 # 后台管理布局（侧栏导航、权限守卫）
│
├── middleware/                   # 路由中间件
│   └── admin.ts                  # 后台权限中间件
│
├── plugins/                      # Nuxt 插件
│   └── api.ts                    # API 插件（$api 全局注入）
│
└── assets/                       # 静态资源
    └── styles/
        └── theme.css             # 全局主题样式
```

### `netlify/` - Netlify 后端

```
netlify/
└── functions/                    # Netlify Functions（Serverless）
    ├── api.ts                    # 单入口 catch-all 路由（/api/*）
    │
    └── _lib/                     # 后端库
        ├── # 核心模块
        ├── auth.ts               # 认证工具（JWT 签发/验证、requireAuth）
        ├── storage.ts            # Netlify Blobs 存储层（getJson/setJson/listKeys/del）
        ├── feed.ts               # 信息流索引（feed.json 维护、用户数据水合）
        ├── notify.ts             # 通知系统（pushNotification、免打扰设置）
        ├── serialize.ts          # 数据序列化（帖子/评论/用户 → 前端格式）
        ├── http.ts               # HTTP 响应工具（json/ok/error/badRequest）
        ├── exp.ts                # 经验值系统（发帖/评论/获赞奖励）
        ├── level.ts              # 等级计算
        ├── glm.ts                # 智谱 GLM AI 调用（Fairy 对话）
        ├── seed.ts               # 数据初始化（首次部署创建管理员）
        └── uid.ts                # UID 生成
        │
        └── routes/               # API 路由模块
            ├── auth.ts           # 认证路由（登录、续期）
            ├── articles.ts       # 帖子路由（CRUD、信息流、搜索、三连）
            ├── comments.ts       # 评论路由（CRUD、@提及通知）
            ├── interactions.ts   # 互动路由（点赞/收藏/关注/拉黑、用户搜索）
            ├── dm.ts             # 私信路由（会话 CRUD、消息收发、群聊管理）
            ├── notifications.ts  # 通知路由（列表、已读、免打扰设置）
            ├── profiles.ts       # 用户资料路由（个人信息、帖子列表）
            ├── uploads.ts        # 上传路由（签名、上传、图片处理）
            ├── admin.ts          # 后台管理路由（用户/帖子/评论/版块/设置）
            ├── ai.ts             # AI 对话路由（会话、消息、流式回复）
            ├── emotes.ts         # 表情包路由（CRUD、分组）
            ├── mihoyo.ts         # 米游社路由（扫码登录、绑定）
            ├── me.ts             # 当前用户路由（个人资料、设置、收藏）
            └── stubs.ts          # 桩接口（在线状态等）
```

### `zzzui/` - 自研 UI 组件库

绝区零风格的 UI 组件库，提供基础组件。

```
zzzui/
├── components/                   # UI 组件
│   ├── ZButton.vue               # 按钮组件
│   ├── ZDropdown.vue             # 下拉菜单
│   ├── ZPattern.vue              # 装饰图案
│   ├── ZMessage.vue              # 消息提示
│   └── ...
├── styles/                       # 样式文件
└── index.ts                      # 入口文件
```

### `public/` - 静态资源

```
public/
├── images/                       # 图片资源
│   ├── default-avatar.webp       # 默认头像
│   ├── default-cover.webp        # 默认封面
│   ├── close-btn.webp            # 关闭按钮图标
│   ├── chat_message_arrow_left.webp  # 聊天气泡箭头（左）
│   ├── chat_message_arrow_right.webp # 聊天气泡箭头（右）
│   ├── Bangboo.gif               # 加载动画
│   ├── materials/                # 素材图片
│   │   └── dennies_v2.webp       # 丁尼图标（已弃用）
│   └── ...                       # 其他图片资源
└── favicon.ico                   # 网站图标
```

### `docs/` - 项目文档

```
docs/
├── 更新日志.md                    # 版本变更记录（Keep a Changelog 格式）
├── 功能文档.md                    # 功能说明文档
├── 项目信息.md                    # 项目信息（本文件）
└── ai设计建议.md                  # AI 设计建议文档
```

### 根目录配置文件

| 文件 | 说明 |
|------|------|
| `netlify.toml` | Netlify 部署配置（构建命令、发布目录、Functions 配置） |
| `nuxt.config.ts` | Nuxt 配置（srcDir、模块、样式、SEO 等） |
| `package.json` | 项目依赖和脚本 |
| `tsconfig.json` | TypeScript 配置 |
| `vitest.config.ts` | Vitest 测试配置 |
| `.env.example` | 环境变量示例 |
| `.gitignore` | Git 忽略规则 |
| `README.md` | 项目说明 |
| `SECURITY.md` | 安全说明 |
| `LICENSE` | 开源许可证 |
| `icon.webp` | 项目图标 |

---

## 技术架构

### 前端架构

```
Nuxt 4 (Vue 3)
├── 状态管理：Pinia
├── 数据获取：TanStack Vue Query
├── UI 组件库：自研 zenless-ui
├── 图片灯箱：lightgallery
├── 轮播组件：embla-carousel-vue
├── Markdown：markdown-it + DOMPurify
├── 代码高亮：highlight.js
└── 图片裁剪：vue-advanced-cropper
```

### 后端架构

```
Netlify Functions v2
├── 单入口路由：api.ts
├── 存储层：Netlify Blobs
│   ├── data store：结构化 JSON
│   └── uploads store：图片字节
├── 认证：JWT (jose)
├── 图片处理：sharp
└── AI 对话：智谱 GLM
```

### 数据存储

| 存储类型 | 用途 | 位置 |
|----------|------|------|
| 用户数据 | 用户信息、设置 | `users/<id>.json` |
| 帖子数据 | 帖子内容、元数据 | `posts/<id>.json` |
| 评论数据 | 评论内容、层级关系 | `comments/<postId>/<id>.json` |
| 会话数据 | 私聊/群聊会话 | `dm/conv/<id>.json` |
| 通知数据 | 站内通知 | `notifications/<userId>/<id>.json` |
| 上传文件 | 图片字节 | `uploads/<hash>.webp` |
| 索引数据 | 信息流、草稿等 | `_indexes/*.json` |

---

## 核心模块

### 1. 认证系统

- **登录方式**：米游社扫码登录 + 密钥登录
- **令牌管理**：JWT，自动续期
- **权限控制**：管理员/普通用户角色

**相关文件**：
- 后端：`netlify/functions/_lib/auth.ts`
- 前端：`app/stores/auth.ts`
- 路由：`netlify/functions/_lib/routes/auth.ts`

### 2. 帖子系统

- **发帖**：Markdown 编辑器，多图上传，B站视频嵌入
- **信息流**：推荐/关注/收藏流，瀑布流虚拟列表
- **互动**：点赞/收藏/举报

**相关文件**：
- 后端：`netlify/functions/_lib/routes/articles.ts`
- 前端：`app/pages/create.vue`, `app/pages/post/[id].vue`
- 组件：`app/components/PostCard.vue`, `app/components/PostOverlay.vue`

### 3. 评论系统

- **楼中楼**：支持多级回复
- **富文本**：图片上传、@提及、表情
- **AI 回复**：评论区 @fairy 触发 AI 回复

**相关文件**：
- 后端：`netlify/functions/_lib/routes/comments.ts`
- 前端：`app/components/CommentItem.vue`, `app/components/CommentBody.vue`

### 4. 私信系统（敲敲）

- **私聊**：一对一聊天
- **群聊**：多人聊天，成员管理
- **AI 对话**：与 Fairy AI 角色对话
- **消息类型**：文本、图片、B站视频

**相关文件**：
- 后端：`netlify/functions/_lib/routes/dm.ts`
- 前端：`app/components/KnockKnockModal.vue`, `app/components/DmMessageItem.vue`, `app/components/DmComposer.vue`
- Composable：`app/composables/useDmConversations.ts`

### 5. 通知系统

- **通知类型**：点赞、评论、@提及、关注
- **免打扰**：全局免打扰、按类型免打扰
- **存储**：Netlify Blobs，每用户独立前缀

**相关文件**：
- 后端：`netlify/functions/_lib/notify.ts`, `netlify/functions/_lib/routes/notifications.ts`
- 前端：`app/pages/profile/[id].vue`（通知 Tab）

### 6. 用户系统

- **个人主页**：名片、统计、帖子列表
- **等级体系**：发帖/评论/获赞累积经验
- **关注/粉丝**：社交关系

**相关文件**：
- 后端：`netlify/functions/_lib/routes/profiles.ts`, `netlify/functions/_lib/routes/interactions.ts`
- 前端：`app/pages/profile/[id].vue`

### 7. 后台管理

- **用户管理**：搜索/添加/禁用/删除用户
- **内容管理**：帖子/评论/举报管理
- **系统设置**：站点配置、功能开关

**相关文件**：
- 后端：`netlify/functions/_lib/routes/admin.ts`
- 前端：`app/pages/admin/*.vue`
- API：`app/composables/useAdminApi.ts`

---

## 组件说明

### 核心组件

| 组件 | 路径 | 说明 |
|------|------|------|
| KnockKnockModal | `app/components/KnockKnockModal.vue` | 敲敲弹窗主组件（私聊/群聊/AI） |
| PostOverlay | `app/components/PostOverlay.vue` | 帖子弹窗（首页点击打开） |
| DmMessageItem | `app/components/DmMessageItem.vue` | 私聊消息气泡 |
| DmComposer | `app/components/DmComposer.vue` | 私聊输入框 |
| CommentItem | `app/components/CommentItem.vue` | 评论组件 |
| PostCard | `app/components/PostCard.vue` | 帖子卡片（信息流） |
| UserHoverCard | `app/components/UserHoverCard.vue` | 用户悬浮名片 |
| MentionPicker | `app/components/MentionPicker.vue` | @提及候选浮层 |
| EmotePicker | `app/components/EmotePicker.vue` | 表情选择面板 |
| BilibiliPlayer | `app/components/BilibiliPlayer.vue` | B站视频播放器 |

### Composables

| Composable | 路径 | 说明 |
|------------|------|------|
| useApi | `app/composables/useApi.ts` | 前端 API 层 |
| useAdminApi | `app/composables/useAdminApi.ts` | 后台 API |
| useDmConversations | `app/composables/useDmConversations.ts` | 私聊会话管理 |
| useLightGallery | `app/composables/useLightGallery.ts` | 灯箱 composable |
| useMentionInput | `app/composables/useMentionInput.ts` | @提及输入逻辑 |
| useEmoteInsert | `app/composables/useEmoteInsert.ts` | 表情插入逻辑 |
| usePresence | `app/composables/usePresence.ts` | 在线状态 |
| usePostModal | `app/composables/usePostModal.ts` | 帖子弹窗状态 |
| useKnockKnockModal | `app/composables/useKnockKnockModal.ts` | 敲敲弹窗状态 |

---

## 开发环境

### 环境要求

- Node.js ≥ 20.18
- npm
- Netlify CLI（本地开发需要）

### 本地开发

```bash
# 安装依赖
npm install --legacy-peer-deps

# 启动开发服务器
netlify dev
```

### 构建部署

```bash
# 构建静态站点
npm run generate

# 预览
npx serve .output/public
```

---

## 代码规范

### 提交信息格式

- 新增功能：`feat: ...`
- 修复：`fix: ...`
- 重构：`refactor: ...`
- 文档：`docs: ...`

### 依赖安装

使用 `npm install --legacy-peer-deps`（存在 peer 冲突）。

### 后端验证

```bash
npx tsc --noEmit netlify/functions/api.ts --module ESNext --moduleResolution bundler --target ES2022 --skipLibCheck --esModuleInterop --resolveJsonModule --types node
npx esbuild netlify/functions/api.ts --bundle --platform=node --format=esm --packages=external --outfile="%TEMP%\api-check.mjs"
```

### 前端验证

```bash
npm run generate
```

---

## 相关链接

- **仓库**：https://github.com/raven-null/inter-knot
- **线上**：https://interknot.netlify.app
- **UI 模板来源**：https://github.com/yinengbei/InterKnot-Web
- **更新日志**：[docs/更新日志.md](./docs/更新日志.md)
- **功能文档**：[docs/功能文档.md](./docs/功能文档.md)
