# ── 前端运行时配置 ──────────────────────────────────
# 后端 API 地址：Netlify 同源部署时留空（/api/* 走 Netlify Functions）。
# 本地开发推荐使用 `netlify dev`，同样同源，留空即可。
NUXT_PUBLIC_API_BASE_URL=
NUXT_PUBLIC_SITE_DOMAIN=example.com

# ── 后端（Netlify Functions）配置 ────────────────────
# JWT 签发密钥，务必修改为随机长字符串（生产环境必须设置）
JWT_SECRET=change-me-to-a-long-random-secret

# 首次部署时自动创建的管理员账号（用密钥登录）
ADMIN_INITIAL_PASSWORD=admin123456

# ── 米游社扫码登录（可选）──────────────────────────────
# 这些是米游社官方 Web 客户端下发的私有参数（社区逆向维护，无官方文档）。
# 获取方法：浏览器打开 https://user.mihoyo.com 登录页 → F12 Network →
# 过滤 passport-api.mihoyo.com，打开 createQRLogin 请求，把请求头 x-rpc-app_id /
# x-rpc-device_id / x-rpc-device_fp 抄到下面。参数会随米游社版本变化而失效。
# 未配置时扫码接口返回 501。
MIHOYO_APP_ID=
# 可选（不填则后端每次随机生成 / 使用默认值）
MIHOYO_DEVICE_ID=
MIHOYO_DEVICE_FP=38d81ab24d683
MIHOYO_SDK_VERSION=2.54.0
MIHOYO_CLIENT_TYPE=4
MIHOYO_GAME_BIZ=plat_cn
